export default function DemoPage() {
  return (
    <main className="gradient-bg min-h-screen px-6 py-12">
      <div className="mx-auto max-w-3xl rounded-3xl border border-white/10 bg-white/5 p-10 text-center">
        <p className="text-xs uppercase tracking-[0.2em] text-white/50">Demo Mode</p>
        <h1 className="mt-4 text-3xl font-semibold">Sample dashboard coming soon</h1>
        <p className="mt-3 text-white/70">
          This space will host mocked repo health data while OAuth setup is in
          progress.
        </p>
      </div>
    </main>
  );
}
